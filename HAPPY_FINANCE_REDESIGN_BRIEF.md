# 开心科技金融公司官网重设计与开发任务书

> 这是一份可直接交给另一个 Codex 新会话执行的完整任务说明。目标是重做当前项目首页，不继承现有页面的视觉设计。

## 1. 任务目标

请在当前项目中重新设计并实现「开心科技金融公司」企业官网首页。

- 公司中文名：开心科技金融公司
- 对外简称：开心科技金融
- 英文名：KAIXIN FINTECH
- 品牌定位：面向成长型企业与金融机构的 AI 原生金融基础设施公司
- 核心主张：让金融更聪明，让增长更从容
- 主要受众：企业 CFO、财务负责人、风控与合规负责人、金融机构产品与技术负责人
- 首要转化目标：预约产品演示
- 次要转化目标：了解产品能力、联系解决方案顾问

参考本地页面：`D:\Project\company\明略科技-全球Agentic AI 第一股.html`

线上参考：https://www.mininglamp.com/

参考其布局节奏与品牌感，但不要复制其 logo、图片、文案、产品名称或代码。最终页面必须有开心科技金融自己的视觉识别。

## 2. 对参考站的设计拆解

需要保留的设计语言：

- 白色顶栏，品牌居左，主导航横向展开，整体安静、克制。
- 约 700px 高的沉浸式深色首屏，左侧大标题、短说明和描边按钮，背景由抽象科技视觉承担品牌情绪。
- 首屏底部叠压一组白色产品入口；桌面端四等分、圆角约 20px、内部用细分隔线组织。
- 主内容以白色与极浅灰交替，大量留白，中心标题清晰，避免过密。
- 关键指标使用高饱和蓝色大数字；能力模块使用三列浅色卡片；产品矩阵用更轻的无边框网格。
- 区块之间依靠背景、间距和排版层级区分，不依赖厚重阴影。
- 桌面端右侧保留轻量的快捷联系入口；移动端完全单列并弱化悬浮控件。

参考站可借鉴的数值节奏：

- 桌面首屏高度：约 700px。
- 首屏标题：约 44px，常规字重。
- 首屏正文最大宽度：约页面的 48%。
- 产品入口容器：最大宽度约 1280–1300px，向上叠压约 96–100px。
- 主区标题：约 36px / 500；移动端约 24–28px。
- 浅灰背景：接近 `#F7F7F7`。
- 参考蓝：接近 `#2279F3`。
- 能力卡：20px 圆角，约 32–36px 内边距。
- 主要断点：1024px、768px。

## 3. 独立视觉方向

设计概念：**「确定性，也可以令人愉悦」**。

金融页面通常只有冷蓝、行情图和玻璃卡片。本项目保留可信的深蓝基调，但加入一束克制的“晨曦橙”，并以“微笑轨道”作为唯一强记忆点：两条数据轨迹在首屏汇聚成一个轻微上扬的弧线，既像资金流，也像微笑。不要使用金币、握手、城市天际线、K 线大屏或普通 SaaS 仪表盘作为主视觉。

独特性原则：

- 大胆只花在首屏的“决策轨道”视觉上，其他区域保持理性、轻盈。
- 蓝色负责可信与行动，晨曦橙只负责温度和关键节点，不作为大面积按钮色。
- 产品能力通过清晰文本和定制线性图标表达，避免把所有内容切成相同圆角卡片。
- 不使用无意义的全大写眉题、编号装饰、霓虹发光、重玻璃拟态或满屏渐变。
- 不做像素级复刻，不使用参考站下载目录中的任何图片或 logo。

### 3.1 色彩令牌

```css
:root {
  --ink-950: #06111f;       /* 首屏深海军蓝 */
  --ink-800: #13243a;       /* 深色正文与页脚 */
  --text-900: #111827;      /* 主正文 */
  --text-600: #5f6b7a;      /* 次正文 */
  --brand-600: #246bfd;     /* 主品牌蓝 */
  --brand-400: #55b8ff;     /* 数据流高光 */
  --joy-500: #ff7a45;       /* 晨曦橙，仅用于轨道节点和小面积强调 */
  --surface-100: #f5f7fa;   /* 浅灰区块 */
  --surface-0: #ffffff;
  --line: #dce2ea;
}
```

正文必须满足 WCAG AA 对比度。浅灰正文不要低于 `#5F6B7A`。

### 3.2 字体与字号

- 中文主字体：优先 `MiSans VF`；项目没有字体文件时使用 `"PingFang SC", "Microsoft YaHei", sans-serif`，不要为了字体引入不稳定的第三方 CDN。
- 英文与数字：`Inter` 或系统无衬线；首屏右侧可用 `Georgia` 显示一句英文品牌句，形成与参考站相似的编辑感，但不要在中文标题中只给某个词变色或斜体。
- H1：桌面 `clamp(44px, 4.2vw, 68px)`，移动端 36px，行高 1.12，字重 500。
- H2：桌面 36px，移动端 26px，行高 1.3，字重 600。
- H3：20px，字重 600。
- 正文：16px，行高 1.75；首屏正文桌面 17–18px。
- 辅助文字：14px，行高 1.6。
- 中文正文单行尽量不超过 36–40 个汉字；桌面文本列最大宽度约 680px。

### 3.3 布局系统

- 页面最大内容宽度：1280px。
- 桌面左右安全边距：48px；平板 32px；手机 20px。
- 使用 12 列网格；主要区块上下间距桌面 112–136px，移动端 72–88px。
- 卡片圆角只用于首屏产品入口和三张核心技术卡；其他产品使用开放式网格，避免“卡片套件”感。
- 阴影只允许用于首屏叠压入口和移动菜单，使用极轻软阴影。

## 4. 页面结构与完整文案

### 4.1 Header

桌面高度 88px，白色背景；滚动后保持吸顶并增加 1px 底边，不要明显阴影。

左侧 logo：新做一个简洁 SVG 标记，由两条轨道弧和一个圆点组成，远看呈轻微微笑；旁边显示：

```text
开心科技金融
KAIXIN FINTECH
```

导航：

- 技术与产品
- 解决方案
- 客户案例
- 开发者中心
- 关于我们
- 洞察

右侧：搜索图标、`EN`、主按钮「预约演示」。

如果当前项目只有单页，导航必须滚动到对应区块，不能出现无效链接。移动端使用 64px 顶栏和可访问的菜单抽屉。

### 4.2 HeroCarousel

高度：桌面 700px；移动端最少 680px。深色背景使用 CSS 径向渐变、SVG 曲线和轻噪点叠层完成，不使用视频。

构图：内容左对齐，桌面宽度 52%；右侧为“决策轨道”视觉和一句大号英文：

```text
Money moves.
Decisions become certain.
```

三张轮播内容：

1. 标题：`让每一笔资金，都更聪明地流动`
   正文：`开心科技金融以多智能体决策引擎连接账户、交易、风险与合规，为成长型企业提供实时、可信、可解释的金融基础设施。`
   主按钮：`预约产品演示`
   次链接：`了解开心智核`

2. 标题：`把风险看见在发生之前`
   正文：`风瞳 RiskLens 以实时金融图谱识别异常交易、关联账户与潜在欺诈，在毫秒级响应中给出可解释的处置建议。`
   主按钮：`了解风控能力`

3. 标题：`一套系统，看清全球现金流`
   正文：`星账 TreasuryOne 汇总多币种账户、预测资金缺口并生成调拨方案，让财务团队每天更早一步做决定。`
   主按钮：`查看资金方案`

轮播规则：6.5 秒自动切换；提供左右箭头和分页点；鼠标悬停、键盘聚焦或用户交互时暂停；`prefers-reduced-motion` 下取消自动播放和背景漂移。

### 4.3 首屏叠压产品入口 ProductRail

桌面端四列、白底、20px 圆角，向上叠压 96px；内容之间用 `#DCE2EA` 细线分隔。不要给每一格单独加阴影。

- `HappyCore 开心智核`：企业级金融决策中枢
- `RiskLens 风瞳`：实时反欺诈与风险预警
- `TreasuryOne 星账`：全球资金与现金流管理
- `RegCloud 合规云`：持续合规与审计智能体

每格包含产品名、一行说明和「查看详情」。桌面 hover 时只让箭头平移 4px、标题变蓝；移动端改为单列列表，不横向溢出。

### 4.4 平台能力数字 MetricBand

标题：`AI 原生金融，从确定性开始`

数字四列，数字使用品牌蓝：

- `99.99%` / 核心服务可用性目标
- `<120ms` / 实时风险响应
- `68%` / 财务流程自动化率
- `7×24` / 智能风险监测

区块底部以小字注明：`以上为产品演示数据，正式发布前请替换为经审核的真实指标。`

不要使用滚动数字跳动效果；进入视口时可做一次 450ms 的透明度显现。

### 4.5 三大技术底座 TechPillars

浅灰背景。标题：`三大技术底座，支撑可信金融决策`

说明：`把实时数据、行业知识与合规规则连接起来，让 AI 不只给出答案，也交代答案从何而来。`

三张白色卡片：

1. `实时金融知识图谱`
   `持续关联账户、交易、主体与事件，帮助团队发现隐藏关系、资金路径和风险传播链。`

2. `多智能体决策引擎`
   `由分析、风控、合规与执行智能体协同工作，在统一规则下完成推理、校验和行动建议。`

3. `隐私计算与审计追踪`
   `敏感数据最小化流动，关键决策全程留痕，支持权限隔离、策略回放和人工复核。`

每张卡使用独立手绘感线性 SVG 图标，按钮为蓝色小胶囊「了解更多」。图标不要直接使用通用图标库原图。

### 4.6 产品矩阵 ProductMatrix

白色背景。标题：`智能金融产品矩阵，覆盖企业关键场景`

副标题：`从交易发生前到结算完成后，让每一次决策都有数据、有规则、有回路。`

使用三列开放网格，项目之间主要靠 72px 以上留白，不使用统一大白卡。共六项：

- `HappyPay 企业支付`：统一收付款、智能路由与对账，让复杂资金链路保持清晰。
- `TreasuryOne 全球司库`：多账户、多主体、多币种资金归集与现金流预测。
- `RiskLens 智能风控`：实时识别欺诈、信用与运营风险，输出可解释证据链。
- `CreditFlow 信贷编排`：把资料采集、授信策略、审批和贷后监测连接为一条流程。
- `RegCloud 合规助手`：跟踪规则变化，自动检查业务策略并形成审计底稿。
- `JoyBI 金融经营分析`：将经营指标、资金成本和增长机会汇总为每日决策简报。

每项包含小型产品符号、标题、2–3 行说明和描边按钮。区块末尾居中放置「查看全部产品」。

### 4.7 行业与信任 ProofSection

用一条细分隔线进入该区块。标题：`复杂业务正在变简单`

数据四列：

- `20+` 数据源连接器
- `12` 行业策略模板
- `6` 主流币种能力
- `3.2×` 对账效率提升

说明：`从零售、电商到制造与跨境业务，开心科技金融把分散的账户、规则和流程连接成可执行的金融系统。`

下方放 5 个纯文字示例客户标识：`北辰零售 / 海岳出行 / 启明制造 / 星帆电商 / 云屿物流`。必须标注「示例客户名称」，避免让虚构内容看起来像真实背书。

### 4.8 最新洞察 Insights

浅灰背景。标题：`最新洞察`

三篇内容，桌面三列，移动端单列：

- `开心智核 2.0 发布：从看见问题到自动编排解决方案` — 2026.08.18
- `企业现金流预测，不应该只是月底的一张表` — 2026.07.05
- `如何为 AI 金融决策建立可追溯证据链` — 2026.05.22

每篇文章使用基于 CSS/SVG 生成的抽象封面，不使用网络随机图片。卡片圆角 16px，但不加重阴影。区块底部放「查看全部洞察」。

### 4.9 转化区 ContactCTA

深蓝全宽区块，左侧：

```text
让复杂金融，变成清晰的下一步
告诉我们你的业务场景，解决方案顾问将在一个工作日内联系你。
```

右侧按钮：`预约产品演示`、`下载产品手册`。

按钮点击如果没有后端：打开本页可访问的联系弹窗，字段包含姓名、公司、工作邮箱、联系电话、需求描述；前端校验完整，但不伪造提交成功。可以展示「演示环境暂未接入提交接口」。

### 4.10 Footer 与快捷联系

页脚四列：

- 产品：开心智核、风瞳、星账、合规云
- 解决方案：智能风控、全球司库、信贷科技、合规科技
- 公司：关于我们、加入我们、新闻与洞察
- 联系：`400-800-2026`、`hello@kaixinfin.example`

底部：`© 2026 开心科技金融公司。演示站点，内容均为虚构。`

桌面右侧可保留窄型快捷栏：电话咨询、在线咨询、返回顶部。移动端隐藏快捷栏，在页面底部提供固定但不遮挡内容的「预约演示」按钮；用户滚到 ContactCTA 后自动隐藏。

## 5. 页面线框

### 桌面

```text
┌──────────────────────────────────────────────────────────────┐
│ Logo        技术与产品  解决方案  客户案例 ...     EN 预约演示 │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  H1 让每一笔资金，都更聪明地流动      决策轨道 / 英文品牌句     │
│  说明文字                                                     │
│  [预约产品演示]  了解开心智核                    ‹        ›     │
│                                                              │
│     ┌────────┬────────┬────────┬────────┐                     │
└─────│ 产品 1 │ 产品 2 │ 产品 3 │ 产品 4 │─────────────────────┘
      └────────┴────────┴────────┴────────┘

              AI 原生金融，从确定性开始
       99.99%        <120ms        68%        7×24

      三大技术底座，支撑可信金融决策  [三张能力卡]

      智能金融产品矩阵                [3 × 2 开放网格]

      复杂业务正在变简单              [数据 + 示例客户]

      最新洞察                        [3 篇文章]

      深蓝 CTA                         [两个按钮]

      Footer
```

### 移动端

```text
┌──────────────────────┐
│ Logo              ☰  │
├──────────────────────┤
│ Hero 标题             │
│ 说明                  │
│ [预约演示]            │
│ 抽象轨道背景           │
├──────────────────────┤
│ 产品 1                │
├──────────────────────┤
│ 产品 2                │
├──────────────────────┤
│ 产品 3                │
├──────────────────────┤
│ 产品 4                │
└──────────────────────┘
  指标 2 × 2
  技术卡单列
  产品单列
  洞察单列
  CTA / Footer
```

## 6. 动效与交互

- 页面加载只做一次编排：顶栏淡入，首屏标题与按钮依次出现，决策轨道随后点亮，总时长不超过 900ms。
- 区块滚动进入只允许轻微透明度变化，不给每一张卡都做上浮动画。
- 轨道节点可缓慢移动，但动画周期不少于 8 秒，避免喧闹。
- 按钮 hover：背景或描边状态明确，位移不超过 2px。
- 产品入口 hover：箭头平移 4px，标题变为品牌蓝。
- 所有交互必须有键盘 focus-visible 状态。
- `prefers-reduced-motion: reduce` 时禁用轮播自动播放、轨道移动和平滑滚动。

## 7. 开发要求

1. 先检查现有项目技术栈与目录结构，延续已有框架、构建工具和代码风格。不要为了这个页面重建项目，也不要无故引入大型依赖。
2. 重做首页视觉，不需要保留原首页组件样式；如果旧页面有业务逻辑或路由能力，保持可用。
3. 推荐组件拆分：

```text
SiteHeader
MobileMenu
HeroCarousel
DecisionOrbit
ProductRail
MetricBand
TechPillars
ProductMatrix
ProofSection
Insights
ContactCTA
ContactDialog
FloatingContact
SiteFooter
```

4. 文案与产品数据放在独立配置对象中，组件通过数据渲染，不要在 JSX 中复制六次近似结构。
5. 使用语义化 HTML：一个 `h1`，区块使用 `section` 与正确层级的标题，导航、轮播和弹窗带完整 aria 属性。
6. 首屏抽象视觉优先使用 CSS 和内联 SVG；不要复制参考站素材，不要远程热链图片。
7. SVG logo 与图标必须是本项目原创的简化图形，颜色通过 `currentColor` 控制。
8. 如果使用 Next.js，图片走框架优化；如果是 Vite/React，静态资源放现有 assets 目录并进行压缩。
9. SEO：

```text
title: 开心科技金融｜AI 原生金融基础设施
description: 开心科技金融以智能风控、全球司库、合规科技与金融决策智能体，帮助企业更清晰地管理资金与增长。
```

10. 不实现虚假后端。联系表单没有接口时明确展示演示态，不要把按钮做成无反应。
11. 不要使用参考 HTML 中的 WordPress/Elementor 结构；重写为当前项目原生组件与样式。

## 8. 响应式规格

- `>= 1280px`：1280px 内容宽度；产品入口四列；产品矩阵三列。
- `768–1279px`：左右 32px；产品入口 2×2；技术卡三列或根据可读性改 2+1；产品矩阵两列。
- `< 768px`：左右 20px；所有内容单列；指标 2×2；隐藏桌面导航和右侧快捷栏。
- 390px 宽度下不得横向滚动；按钮触控高度至少 44px；移动菜单可关闭、可按 Escape 退出、关闭后恢复焦点。
- 轮播内容高度允许随文案增长，不要使用固定高度导致中文截断。

## 9. 性能与质量底线

- 首屏不使用背景视频；初始关键资源控制体积。
- 避免布局抖动，为首屏和文章封面保留明确尺寸。
- 图片延迟加载，首屏关键视觉除外。
- 目标：LCP < 2.5s、CLS < 0.1；若本地环境无法测量，至少确保构建产物无明显大资源和阻塞脚本。
- 无浏览器控制台错误、无缺图、无无效锚点、无文本溢出。
- 桌面 1440×900、平板 1024×768、手机 390×844 三种尺寸均需截图审查。
- 最终阶段再统一运行项目已有的 lint、typecheck、build 或测试命令；实现过程中不要边改边跑测试。

## 10. 完成验收标准

- 打开首页第一眼能联想到参考站的“大首屏 + 悬浮产品入口 + 大数字 + 宽松矩阵”节奏，但看不出复制其品牌资产。
- 页面明确显示「开心科技金融」和 `KAIXIN FINTECH`，不存在明略科技相关文本或图片。
- 首屏“微笑轨道”成为唯一主要视觉记忆点，蓝色可信、橙色克制。
- 所有主要按钮都有真实可见反馈；单页导航、轮播、移动菜单、联系弹窗、返回顶部均可用。
- 页面在 390px 宽度下完整可读，无横向滚动和遮挡。
- 键盘可完成导航、轮播控制和弹窗操作；减少动态偏好生效。
- 构建成功，项目原有非首页功能不被破坏。

## 11. 执行方式

请直接开始实施，不要只输出设计建议。先检查仓库和现有技术栈，再完成组件、样式、原创 SVG 视觉、响应式和交互；全部实现结束后进入独立验证阶段，运行项目已有检查并用桌面与移动截图复核。若现有代码与本任务书冲突，以“重做首页视觉但不破坏项目基础设施”为原则处理。
